# Dipakai docker-compose.yml (docker-compose otomatis load .env di direktori
# yang sama). Copy jadi `.env` (JANGAN commit .env yang isinya password asli --
# sudah di-gitignore) dan isi dengan password produksi sesungguhnya.
POSTGRES_PASSWORD=changeme
